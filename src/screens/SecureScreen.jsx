import { StyleSheet, Text, View } from 'react-native';
import React from 'react';

const SecureScreen = () => {
  return (
    <View>
      <Text>SecureScreen</Text>
    </View>
  );
};

export default SecureScreen;

const styles = StyleSheet.create({});
